// chrome.tabs.executeScript(tabs[0].id, { code: `screenPos = JSON.stringify({offsetY: window.screen.height - window.innerHeight, offsetX: window.screen.width - window.innerWidth}); let offsetScreen = JSON.parse(screenPos); offsetScreen` },
//     function (results2) { console.log(results2); });




